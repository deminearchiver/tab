{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "EXTENSION_BROWSER_TARGET"
  ],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
